from .colors import *

style = """
            QTextBrowser {
                background-color: %s;
                color: %s;
                border: %s;
            }
        """ % (normal_background, normal_color, border)