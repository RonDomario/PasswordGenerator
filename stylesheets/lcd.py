from .colors import *

style = """
            QLCDNumber {
                background-color: %s;
                color: %s;
                border: %s;
            }
        """ % (normal_background, normal_color, border)